# at the end of every interaction, curate the interaction content, label it with user, thread, etc, and store it in pinecone
# extract defined entities from the interaction content
# at agentend interaction
